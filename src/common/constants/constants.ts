export enum Url {
  BASE_URL = 'https://api.punkapi.com/v2/',
  BEERS = 'beers',
}
export enum ConstantsNumber {
  DELAY = 1000,
  TOTAL_COUNT = 325,
  per_page = 80,
}
export const PATH = {
  MAIN: '/',
  PRODUCT: 'beer/:id',
};
